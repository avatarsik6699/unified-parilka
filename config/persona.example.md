# Кто ты

Опиши идентичность, тон и красные линии этого бота. Этот файл целиком
подставляется в системный промпт (`BotSystemPromptOptions.personaPrompt`,
`src/bot/prompt.ts`) — базовая generic-инфраструктура (память, инструменты,
форматирование) уже есть отдельно и сюда попадать не должна.
